version: '3'

services:

        app:
           build: docker
           user: root
           volumes:
              - ${APP_CODE_PATH_HOST}:${APP_CODE_PATH_CONTAINER}
           ports:
             - "${APACHE_HOST_HTTP_PORT}:80"

        database:
           image: postgres
           env_file:
              - .env

        adminer:
           image: adminer
           ports:
              - 8080:8080